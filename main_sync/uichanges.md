Title should be: "Pediatric Discharge Instruction Review"

Below that in 2 lines it should say Patient Name: John Doe MRN: (id) Age: x years Sex: M/F (Since you do not know name and sex just hardcode in John Doe and M for each)

Below that slightly larger should be the Module that is selected

Make the Spanish checkbox just say Generate Spanish; then when you hover over ? it tells you the whole phrase

After Generating Instructions it should look like this:

We want to grab the edge of the clinical orders box and simplified English box so we can drag them to change the width of each box. This should be one drag that adjusts both boxes

Simplified English (Clinical editor both boxes should be the same length
remove the "inline clinical editor: " line on the simplified english
Failed safety tokens should be in a box that is trimmed red

Left Side Bar:
Under #3 "Module:" Should Be "Diagnosis:"

Lets also shift down everything from 3-4 to 4-5 and return #2. GitHub app; but this should just show red/green if connected or not and a refresh button to the side (keep this short)

Remove this: LLM1 simplifies English to FKGL 5.0–6.9. LLM2 checks safety. Spanish translation and English back-translation run only when requested for the family.

"Synthetic Patient ID" should be "MRN"

PDF Printout:
Spanish version should have Spanish first then Original simplified english (not side by side on page one after the other)


Other edits:
remove deploy button
During rejection; questions should be optional
